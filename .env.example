DATABASE_URL="postgresql://postgres:desarrollo_software_1@localhost:5432/desarrollo_software_1?schema=public"

# Usadas por docker-compose para levantar el servicio `db` y construir el DATABASE_URL
# del servicio `app` dentro de la red de Docker (host `db` en vez de `localhost`).
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

JWT_SECRET=
JWT_EXPIRES_IN=
UF_API_URL=https://mindicador.cl/api/uf
PORT=3000
